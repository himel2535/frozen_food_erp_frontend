{
  "name": "hookerp",
  "version": "1.0.0",
  "private": true,
  "description": "Food Fun Agro Foods — Next.js frontend",
  "scripts": {
    "dev": "cross-env npm_config_devdir= npm run dev --prefix web",
    "build": "cross-env npm_config_devdir= npm run build --prefix web",
    "start": "cross-env npm_config_devdir= npm run start --prefix web",
    "lint": "cross-env npm_config_devdir= npm run lint --prefix web",
    "analyze": "cross-env npm_config_devdir= npm run analyze --prefix web",
    "deploy:rules": "node web/scripts/deploy-rtdb-rules.mjs",
    "cleanup:orphan-auth": "cross-env npm_config_devdir= npm run cleanup:orphan-auth --prefix web"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
