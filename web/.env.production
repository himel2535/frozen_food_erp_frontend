# Public Cloudinary unsigned-upload config (NEXT_PUBLIC — inlined into the browser bundle).
# Vercel / `next build` load this file. Dashboard env vars override these if set.
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=monwar-hossan-himel
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=toys_factory_erp_preset
